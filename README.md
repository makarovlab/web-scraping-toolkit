# web-scraping-toolkit
Streamlit UI for Webscraping and Quality Assurance

## Dependencies
`pipenv install`

## Run server
`pipenv run streamlit run main.py`

## Run with port
`pipenv run streamlit run main.py --server.port=9999`